vich_uploader:
    db_driver: orm
    storage: flysystem

    mappings:
        multimedia:
            upload_destination: multimedia.storage
            uri_prefix: '%env(SCALEWAY_MULTIMEDIA_BUCKET_ENDPOINT)%'
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
