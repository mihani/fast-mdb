<form method="post" class="justify-content-start" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sur de supprimer cet utilisateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">{{ 'admin.user.button.delete'|trans }}</button>
</form>
